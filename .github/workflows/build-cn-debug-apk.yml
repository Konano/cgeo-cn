name: Build c:geo CN debug APK

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cgeo-cn-debug-apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v6

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v5

      - name: Restore persistent debug keystore
        shell: bash
        env:
          DEBUG_KEYSTORE_BASE64: ${{ secrets.DEBUG_KEYSTORE_BASE64 }}
        run: |
          if [ -z "$DEBUG_KEYSTORE_BASE64" ]; then
            echo "::error::DEBUG_KEYSTORE_BASE64 is not configured"
            exit 1
          fi

          mkdir -p "$HOME/.android"
          printf '%s' "$DEBUG_KEYSTORE_BASE64" \
            | base64 --decode \
            > "$HOME/.android/debug.keystore"

          chmod 600 "$HOME/.android/debug.keystore"

          keytool -list \
            -keystore "$HOME/.android/debug.keystore" \
            -storepass android \
            -alias androiddebugkey \
            > /dev/null

      - name: Configure c:geo CN debug APK
        shell: bash
        run: |
          sed -i \
            -e "s|date.format('yyyy.MM.dd')|date.format('yyyy.MM.dd.HH.mm.ss')|" \
            -e '/\/\/ Leak Canary, memory leak detection/d' \
            -e "/String leakCanaryVersion = /d" \
            -e '/debugImplementation "com.squareup.leakcanary:leakcanary-android:/d' \
            -e '/implementation "com.squareup.leakcanary:plumber-android:/d' \
            main/build.gradle

          sed -i \
            's|<string translatable="false" name="app_name">c:geo</string>|<string translatable="false" name="app_name">c:geo debug</string>|' \
            main/src/main/res/values/strings_not_translatable.xml

          rm main/src/debug/java/cgeo/geocaching/utils/buildconfig/DebugBuildConfigHooks.java

          if grep -qi 'leakcanary' main/build.gradle; then
            echo "::error::Failed to remove LeakCanary dependencies"
            exit 1
          fi

          grep -q "date.format('yyyy.MM.dd.HH.mm.ss')" main/build.gradle
          grep -q 'versionNameSuffix = "-$commitIdShort developer build"' main/build.gradle
          grep -q '<string translatable="false" name="app_name">c:geo debug</string>' \
            main/src/main/res/values/strings_not_translatable.xml

      - name: Build BasicDebug APK
        shell: bash
        env:
          TZ: Asia/Shanghai
        run: |
          chmod +x ./gradlew
          ./gradlew --no-daemon assembleBasicDebug

      - name: Upload APK
        uses: actions/upload-artifact@v7
        with:
          name: cgeo-cn-debug-apk
          path: main/build/outputs/apk/basic/debug/*.apk
          if-no-files-found: error
          retention-days: 30
